extends layout

block content
  h1= title
  p Welcome to #{title}, I am lazy
  button(type="button", class="btn btn-primary") Press Me
  div#faster Increase the speed

  script. 
    var mph=20;
    var change =1;
    function goFaster(){
      mph+=change;
      change=change+1;
      document.getElementById('faster').innerText= `MPH ${mph} with change ${change}`;
    }
  br
  br
  a(href="http://localhost:3000/furniture") Search Results